## 2026-07-22 - GitHub Updates
**Инсайт:** Логика проверки обновлений частично дублировалась в CLI и core. `reqwest` зависимость отсутствовала feature "json" в bsdpi-core.
**Действие:** Завершать функции-заглушки (SelfUpdater в bsdpi-core) и переносить в них дублирующийся код для сохранения чистоты архитектуры.

## 2026-07-25 - AiOrchestratorService State Machine
**Инсайт:** При реализации `AiOrchestratorService` была выявлена необходимость использовать асинхронные блокировки (`tokio::sync::Mutex` вместо стандартных `std::sync::Mutex`). Если используются стандартные мьютексы, компилятор не позволит передавать их через точки `await`, так как они не реализуют `Send` безопасно для асинхронного контекста. Это критично для конечных автоматов, которые выполняют асинхронные операции в своих состояниях.
**Действие:** Всегда использовать `tokio::sync::Mutex` или `RwLock` в асинхронных стейт-машинах и сервисах (особенно в `bsdpi-ai` и `bsdpi-core`), где требуется удержание блокировки через асинхронные вызовы.
